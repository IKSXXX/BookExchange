@model HomeViewModel
@inject SignInManager<User> SignInManager
@{
    ViewData["Title"] = "Главная";
}

<section class="hero">
    <div class="hero-inner">
        <span class="hero-eyebrow">
            <svg class="icon"><use href="#i-sparkles"/></svg>
            Новое поколение книжного обмена
        </span>
        <h1>Обменивайтесь книгами<br/><em>с единомышленниками</em></h1>
        <p>
            BookSwap — это сообщество любителей чтения.
            Находите редкие издания, делитесь своими книгами
            и открывайте новые миры через страницы историй.
        </p>
        <div class="hero-actions">
            <a asp-controller="Book" asp-action="Index" class="btn btn-primary btn-lg">
                <svg class="icon"><use href="#i-search"/></svg> Найти книгу
            </a>
            @if (!SignInManager.IsSignedIn(User))
            {
                <a asp-controller="Account" asp-action="Register" class="btn btn-outline btn-lg">
                    Присоединиться <svg class="icon"><use href="#i-arrow-right"/></svg>
                </a>
            }
            else
            {
                <a asp-controller="Book" asp-action="Create" class="btn btn-outline btn-lg">
                    <svg class="icon"><use href="#i-plus"/></svg> Добавить книгу
                </a>
            }
        </div>
        <div class="hero-metrics">
            <div class="hero-metric">
                <div class="hero-metric__value">1,200+</div>
                <div class="hero-metric__label">книг в каталоге</div>
            </div>
            <div class="hero-metric">
                <div class="hero-metric__value">450+</div>
                <div class="hero-metric__label">активных читателей</div>
            </div>
            <div class="hero-metric">
                <div class="hero-metric__value">890+</div>
                <div class="hero-metric__label">успешных обменов</div>
            </div>
        </div>
    </div>
</section>

<section class="section">
    <div class="features-grid">
        <div class="feature-tile">
            <div class="feature-tile__icon feature-tile__icon--green">
                <svg class="icon"><use href="#i-book"/></svg>
            </div>
            <h3>Большой каталог</h3>
            <p>Тысячи книг от читателей со всей страны — классика, новинки, редкие издания.</p>
        </div>
        <div class="feature-tile">
            <div class="feature-tile__icon feature-tile__icon--orange">
                <svg class="icon"><use href="#i-swap"/></svg>
            </div>
            <h3>Простой обмен</h3>
            <p>Выбирайте книги, предлагайте свои — и договаривайтесь о встрече прямо в чате.</p>
        </div>
        <div class="feature-tile">
            <div class="feature-tile__icon feature-tile__icon--lime">
                <svg class="icon"><use href="#i-users"/></svg>
            </div>
            <h3>Сообщество</h3>
            <p>Знакомьтесь с читателями, обсуждайте книги и находите новые литературные рекомендации.</p>
        </div>
        <div class="feature-tile">
            <div class="feature-tile__icon feature-tile__icon--amber">
                <svg class="icon"><use href="#i-sparkles"/></svg>
            </div>
            <h3>AI-помощник</h3>
            <p>Умный помощник подберёт книги под ваши вкусы и расскажет, что почитать следующим.</p>
        </div>
    </div>
</section>

<section class="section">
    <div class="home-features">
        @* --- КНИГА ДНЯ --- *@
        <div class="panel feature-card feature-card--day">
            <span class="eyebrow">Книга дня</span>
            <h2 class="section-title">
                <svg class="icon"><use href="#i-bookmark"/></svg>
                Сегодня рекомендуем
            </h2>
            @if (Model.BookOfTheDay != null)
            {
                <div class="book-of-day">
                    <partial name="_BookCard" model="Model.BookOfTheDay" />
                </div>
            }
            else
            {
                <div class="empty">
                    <svg class="icon icon-xl"><use href="#i-bookmark"/></svg>
                    <p>Сегодня админ ещё не выбрал книгу дня.</p>
                </div>
            }
        </div>

        @* --- МИНИ-ИГРА --- *@
        <div class="panel feature-card feature-card--quiz">
            <span class="eyebrow">Мини-игра</span>
            <h2 class="section-title section-title--accent">
                <svg class="icon"><use href="#i-sparkles"/></svg>
                Угадай книгу по цитате
            </h2>
            <div id="quizWidget" data-initial='@Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.QuizQuestion))'>
                <p class="text-muted">Загрузка…</p>
            </div>
        </div>
    </div>
</section>

<section class="section">
    <div class="page-header page-header--slim">
        <div>
            <span class="eyebrow">Свежее поступление</span>
            <h2 class="section-title" style="margin: 0;">
                <svg class="icon"><use href="#i-sparkles"/></svg>
                Недавно добавленные
            </h2>
        </div>
        <a asp-controller="Book" asp-action="Index" class="btn btn-ghost btn-sm">
            Все книги <svg class="icon"><use href="#i-arrow-right"/></svg>
        </a>
    </div>
    <div class="catalog-grid">
        @foreach (var b in Model.RecentBooks)
        {
            <partial name="_BookCard" model="b" />
        }
    </div>
</section>

@section Scripts {
    <script>
        // Quiz widget
        (function() {
            const widget = document.getElementById('quizWidget');
            if (!widget) return;
            let initial = null;
            try { initial = JSON.parse(widget.dataset.initial); } catch { }
            function render(q) {
                if (!q) {
                    widget.innerHTML = '<div class="empty"><p class="text-muted">Пока нет вопросов для игры.</p></div>';
                    return;
                }
                const opts = q.options.map(o =>
                    `<button type="button" class="quiz-option" data-ans="${o.replace(/"/g,'&quot;')}">${o}</button>`
                ).join('');
                widget.innerHTML = `
                    <div class="quiz-widget-card">
                        <blockquote class="quiz-quote">«${q.quote}»</blockquote>
                        <div class="quiz-options">${opts}</div>
                        <div class="quiz-result text-sm"></div>
                    </div>`;
                widget.querySelectorAll('.quiz-option').forEach(btn => {
                    btn.addEventListener('click', async () => {
                        const answer = btn.dataset.ans;
                        try {
                            const fd = new FormData();
                            fd.append('questionId', q.id);
                            fd.append('answer', answer);
                            const res = await fetch('/quiz/answer', { method: 'POST', body: fd });
                            const data = await res.json();
                            widget.querySelectorAll('.quiz-option').forEach(b => b.disabled = true);
                            if (data.isCorrect) {
                                btn.classList.add('correct');
                                widget.querySelector('.quiz-result').innerHTML = '<span class="chip chip--ok">✓ Верно!</span>';
                            } else {
                                btn.classList.add('wrong');
                                widget.querySelectorAll('.quiz-option').forEach(b => {
                                    if (b.dataset.ans === data.correctAnswer) b.classList.add('correct');
                                });
                                widget.querySelector('.quiz-result').innerHTML = '<span class="chip chip--danger">Верный ответ подсвечен зелёным</span>';
                            }
                            setTimeout(async () => {
                                const r = await fetch('/quiz/next');
                                if (r.ok) render(await r.json());
                            }, 2500);
                        } catch (e) { console.error(e); }
                    });
                });
            }
            render(initial);
        })();
    </script>
}
